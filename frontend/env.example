# ===========================================
# MEDIX FRONTEND CONFIGURATION
# ===========================================
# Copy this file to .env and update values
# DO NOT commit .env file to git!
# ===========================================

# API Configuration
# Backend API base URL (without /api suffix, apiClient will add it)
VITE_API_BASE_URL=http://localhost:5123/api

# Google OAuth (Optional - only if using Google login)
# Get from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# ===========================================
# NOTES FOR TEAM:
# - Sau khi clone project, copy file này thành .env
# - Cập nhật VITE_API_BASE_URL nếu backend chạy port khác
# - Mọi service đều dùng apiClient tự động
# - Token được quản lý tự động, không cần config thêm
# ===========================================
